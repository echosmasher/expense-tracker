# @typescript-eslint v7 declares a peer dep on eslint 8 but works with eslint 9,
# which this repo uses. Without this flag, npm ci / npm install refuse to resolve.
# Remove once @typescript-eslint is upgraded to v8 (native eslint 9 support).
legacy-peer-deps=true
